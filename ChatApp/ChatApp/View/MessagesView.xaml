<UserControl x:Class="ChatApp.View.MessagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            


        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>

            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        
        <TextBox x:Name="textbox1" Grid.Row="1" Text="{Binding Text1}"    Grid.Column="0"/>
        <Button Content="Button"   Grid.Row="1" Grid.Column="1" Command="{Binding SendMessageCommand}" />
        <ListBox x:Name="listBox1" ItemsSource="{Binding List1}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>

    </Grid>
</UserControl>
